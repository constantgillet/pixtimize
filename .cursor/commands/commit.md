Create a git commit for the current changes.

Read and follow the **Creating a commit (`/commit`)** section in the
`pixtimize-releases` skill (`.cursor/skills/pixtimize-releases/SKILL.md`).
Do not invent a different commit workflow.
